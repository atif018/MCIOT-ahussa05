<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sephiros.wifilogger">

    // https://developer.android.com/guide/topics/permissions/overview used as a reference as
    // well as https://developer.android.com/reference/android/Manifest.permission#ACCESS_FINE_LOCATION

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/> // Permission to access precise location
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/> //Permission to access rough location
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/> // Permission to access WiFi network information
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/> Permission to change Wifi state
    <uses-permission android:name="android.permission.INTERNET"/> // Permission to access Internet




    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>